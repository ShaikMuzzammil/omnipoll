// CreatePoll.tsx — alias page that re-exports Create
// Some project routers reference /create-poll → this page
import React from "react";
import Create from "@/pages/Create";
export default Create;
